const canvas = document.querySelector('canvas');
const c = canvas.getContext('2d');

canvas.width = innerWidth;          //innerWidth is "window.innerwitdth". it's the same thing in javascript.
canvas.height = innerHeight;